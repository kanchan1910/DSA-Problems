class Solution {
public:
    int singleNumber(vector<int>& nums)
    {
        int n = nums.size();
        int xxor = 0;
        for(int i = 0; i < n; i++)
        {
            xxor ^= nums[i];
        }
        return xxor;
    }
};
tc o(n) 
sc o(1)

other methods are using sorting, hashing
